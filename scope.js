let bonus = 20;

function sum(first, second) {
    let result = first + second + 20;
    // console.log (result);
    if ( result > 9) {
        const mood = "nai";
        mood = "ache";
        // console.log(mood);
    }
    console.log(mood);
    return result;
}

// const output = sum (20, 10);
console.log(mood);
// console.log(output);
